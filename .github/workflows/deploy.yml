name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./berutek.ui
          file: ./berutek.ui/Dockerfile
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }}
            NEXT_PUBLIC_RECAPTCHA_SITE_KEY=${{ secrets.NEXT_PUBLIC_RECAPTCHA_SITE_KEY }}
            NEXT_PUBLIC_RECAPTCHA_SECRET_KEY=${{ secrets.NEXT_PUBLIC_RECAPTCHA_SECRET_KEY }}
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}

  build-serv:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./berutek.serv
          file: ./berutek.serv/Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ github.repository }}-serv:latest
            ghcr.io/${{ github.repository }}-serv:${{ github.sha }}

  deploy:
    needs: [build, build-serv]
    runs-on: self-hosted
    steps:
      - name: Login to GitHub Container Registry
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Write server environment
        env:
          # Database
          SERV_DB_HOST: ${{ secrets.DB_HOST }}
          SERV_DB_PORT: ${{ secrets.DB_PORT }}
          SERV_DB_USERNAME: ${{ secrets.DB_USERNAME }}
          SERV_DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          SERV_DB_NAME: ${{ secrets.DB_NAME }}
          # Auth secrets
          SERV_SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          SERV_JWT_ACCESS_SECRET: ${{ secrets.JWT_ACCESS_SECRET }}
          SERV_JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          SERV_ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
          # OIDC
          SERV_OIDC_CLIENT_ID: ${{ secrets.OIDC_CLIENT_ID }}
          SERV_OIDC_CLIENT_SECRET: ${{ secrets.OIDC_CLIENT_SECRET }}
          SERV_OIDC_ISSUER_URL: ${{ secrets.OIDC_ISSUER_URL }}
          SERV_OIDC_DISCOVERY_URL: ${{ secrets.OIDC_DISCOVERY_URL }}
          SERV_OIDC_CALLBACK_URL: ${{ secrets.OIDC_CALLBACK_URL }}
          # Application
          SERV_APP_CORS_ORIGIN: ${{ secrets.APP_CORS_ORIGIN }}
          # Mail
          SERV_SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SERV_SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SERV_SMTP_USER: ${{ secrets.SMTP_USER }}
          SERV_SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SERV_SMTP_FROM: ${{ secrets.SMTP_FROM }}
        run: |
          {
            echo "NODE_ENV=production"
            echo "PORT=3030"
            echo "API_PREFIX=/api/v1"
            echo "DB_HOST=${SERV_DB_HOST}"
            echo "DB_PORT=${SERV_DB_PORT}"
            echo "DB_USERNAME=${SERV_DB_USERNAME}"
            echo "DB_PASSWORD=${SERV_DB_PASSWORD}"
            echo "DB_NAME=${SERV_DB_NAME}"
            echo "DB_SYNCHRONIZE=false"
            echo "DB_LOGGING=false"
            echo "SESSION_SECRET=${SERV_SESSION_SECRET}"
            echo "JWT_ACCESS_SECRET=${SERV_JWT_ACCESS_SECRET}"
            echo "JWT_ACCESS_EXPIRATION=15m"
            echo "JWT_REFRESH_SECRET=${SERV_JWT_REFRESH_SECRET}"
            echo "JWT_REFRESH_EXPIRATION=7d"
            echo "JWT_ISSUER=berutek"
            echo "ENCRYPTION_KEY=${SERV_ENCRYPTION_KEY}"
            echo "APP_CORS_ORIGIN=${SERV_APP_CORS_ORIGIN}"
            echo "OIDC_CLIENT_ID=${SERV_OIDC_CLIENT_ID}"
            echo "OIDC_CLIENT_SECRET=${SERV_OIDC_CLIENT_SECRET}"
            echo "OIDC_ISSUER_URL=${SERV_OIDC_ISSUER_URL}"
            echo "OIDC_DISCOVERY_URL=${SERV_OIDC_DISCOVERY_URL}"
            echo "OIDC_CALLBACK_URL=${SERV_OIDC_CALLBACK_URL}"
            echo "SMTP_HOST=${SERV_SMTP_HOST}"
            echo "SMTP_PORT=${SERV_SMTP_PORT}"
            echo "SMTP_USER=${SERV_SMTP_USER}"
            echo "SMTP_PASSWORD=${SERV_SMTP_PASSWORD}"
            echo "SMTP_FROM=${SERV_SMTP_FROM}"
          } > /opt/berutek/.env.serv

      - name: Pull and restart UI
        run: |
          cd /opt/berutek
          docker compose pull app
          docker compose up -d --no-deps app

      - name: Pull and restart server
        run: |
          cd /opt/berutek
          docker compose pull serv
          docker compose up -d --no-deps serv
